# Runtime

Archivos generados durante la ejecucion local.

Subcarpetas esperadas:

- `script-runs/`: inventario real, reservas VNC/VLAN/CIDR y resultados del driver Linux.
- `jobs/`: reservado si se migra el volumen Docker de jobs a carpeta local.
- `novnc-tokens/`: reservado si se migra el volumen Docker de tokens noVNC a carpeta local.

No debe usarse para codigo fuente.
