# Deploy

Espacio reservado para configuraciones de despliegue.

Por ahora `docker-compose.yml` se mantiene en la raiz del proyecto para conservar el flujo actual:

```bash
sudo docker compose up -d
```

En fase 2 esta carpeta puede alojar archivos `.env`, perfiles de Compose por cluster, scripts de bootstrap y configuracion por ambiente.
